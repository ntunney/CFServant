interface  hint="Adds functions for soft deleting an object.  Also handles soft deletion (or deletion) of all composites."
{
	/* 
		This is coming in a future version.  All the code is written in another project, I just need 
		the time to pull it out and add some additional checks and balances.  I feel that right now 
		it is more important to get the CFServant base working and gather a following, and then add 
		this later.
	 */
	 
}